{
  "name": "patternfly-java-showcase",
  "private": true,
  "source": "src/web/index.html",
  "scripts": {
    "watch": "parcel serve --https --dist-dir target/showcase --open",
    "dev": "parcel build --https --dist-dir target/showcase --no-optimize",
    "prod": "parcel build --dist-dir target/showcase --public-url /showcase"
  },
  "devDependencies": {
    "@parcel/reporter-cli": "^2.12.0",
    "parcel": "^2.12.0"
  },
  "dependencies": {
    "@patternfly/patternfly": "^5.2.1"
  }
}
